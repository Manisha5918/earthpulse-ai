'use client';

import React from "react";

export default function GridLayer({ cells = [], onCellClick, selectedCellCode }) {
  return null; // Handled directly in MapContainer canvas/Leaflet GeoJSON
}
